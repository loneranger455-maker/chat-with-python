first run the server:
python -u (address of file)/server.py
then run the client program
more than 1 users can connect from the same computer and same ip
for external computer to connect please edit the code on clientgui.py set the ip to that of the server computer
